# my-fin Backend

FastAPI scaffold for the future cloud components:

- Auth and JWT session controls
- PowerSync/PostgreSQL bridge
- Tier 2 parser fallback
- Anonymous telemetry batch uploads

The PRD keeps backend work later in the roadmap, so this service currently exposes only a health route.
